<li class="list-group-item">
    {{burger_name}}
        
        {{#if devour}}
        {{else}}
        <button type= "button" class="devour-burger btn btn-primary btn-sm" data-id="{{this.id}}" data-newDevour="{{devour}}">Devour!</button>
        {{/if}}
</li> 